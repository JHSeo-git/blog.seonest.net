export const metadata = {
  title: "Use Agents First",
  description: "코딩 에이전트를 어떻게 쓰는지 정리한 개인적인 경험입니다.",
  alternates: {
    canonical: "/use-agents-first",
  },
  openGraph: {
    type: "article",
    publishedTime: "2026-01-03T11:06:37Z",
  },
}

import planMode from "./plan-mode-0.webp"

# Use Agents First

<PostMeta createdAt={metadata.openGraph.publishedTime} updatedAt={metadata.openGraph.modifiedTime} />

코딩 에이전트와 함께하는 개발은 몇 달 전과 비교해도 180도 달라졌습니다.

불과 1년 전만 해도 모델은 스스로 작업을 이어가지 못했습니다. 도구 호출에 서툴렀고, 없는 코드를 예사로 지어냈습니다.
최신 모델은 아는 것이 많아졌을 뿐 아니라, 지시를 잘 따르고 도구도 정확하게 골라 씁니다.

Cursor, Codex, Claude Code, Antigravity 같은 코딩 에이전트 서비스는 이제 이 모델의 성능을 훨씬 잘 끌어냅니다.
컨텍스트가 가득 차면 모델이 스스로 요약하고 이어서 일하게 하고, 도구 정의와 응답 크기를 줄여 컨텍스트를 아끼고, 파일 시스템에서 필요한 컨텍스트만 그때그때 읽어 옵니다. 이런 작은 손질이 쌓여 큰 차이를 만듭니다.

코딩 에이전트 서비스를 사용하는 개발자들은 현재 도구의 강점과 한계를 배우고 있습니다. 세부적인 계획을 먼저 세운 뒤 코드를 생성하는 것이 결과를 크게 개선합니다.
내년에 훨씬 더 많은 개발자들이 AI로 코드를 작성할 것으로 예상합니다.

저는 Cursor에서 Plan 모드로 계획을 세우고, Build 모드로 구현을 맡깁니다.

## 모델 선택 전략

Opus 4.5와 Codex(ex: GPT-5.1 Codex Max)를 번갈아가면서 사용했습니다.

| 모델         | 특징              | 사용 시점               |
| ------------ | ----------------- | ----------------------- |
| **Opus 4.5** | 글쓰기, 계획 수립 | Plan 모드, 문서 작성    |
| **Codex**    | 장시간 작업       | Build 모드, 대규모 구현 |

Opus 4.5는 글쓰기에 뛰어납니다. 계획을 세우거나, 문서를 작성하거나, 코드 설명이 필요할 때 사용합니다.

Codex는 장시간 작업에 유리합니다. [Lee Robinson](https://leerob.com/pixo)은 에이전트가 한 번에 3시간을 내리 돈 적도 있다고 썼습니다. 자리를 비운 동안에도 에이전트는 작업을 이어갑니다.

## 워크플로우

복잡한 개발 업무는 Plan과 Build 두 단계로 나눕니다. 먼저 Plan 모드에서 계획을 세우고, 에이전트의 추가 질문에 답하며 계획을 다듬습니다. 더 물을 것이 없어지면 Build 모드로 넘어가 구현을 맡깁니다.

## Plan 모드

<Video src="/post/use-agents-first/agent-todo.mp4" />

[Plan 모드](https://cursor.com/docs/agent/planning)는 Cursor에서 가장 과소평가된 기능 중 하나입니다.

Plan 모드는 코드를 작성하기 전에 상세한 구현 계획을 만듭니다. 에이전트가 질문과 컨텍스트를 분석하여 필요한 사항을 확인하며, 빌드 전에 편집할 수 있는 계획을 생성합니다.
구조화된 Todo 리스트와 메시지 큐를 사용해 선제적으로 계획하고 복잡한 작업을 관리하여, 장시간 진행되는 과제를 더 쉽게 파악하고 추적할 수 있습니다.

Plan 모드는 **Opus 4.5**로 돌립니다. 계획서는 결국 글이라, 글을 잘 쓰는 모델이 더 촘촘한 계획을 만듭니다.

## Build 모드

<Image src={planMode} alt="Cursor Plan 모드가 만든 구현 계획" />

Plan 모드에서 수립한 계획을 코딩 에이전트가 수행합니다.

Build를 실행할 때는 **Codex** 모델을 사용합니다. 장시간 작업에 유리하기 때문입니다. 클라우드(샌드박스)에서 에이전트를 실행할 수 있습니다. 다른 일을 하다가 돌아와서 PR을 확인하고 머지하는 식으로 작업할 수 있습니다.

### 클라우드 에이전트

[클라우드 에이전트](https://cursor.com/ko/docs/cloud-agent)는 분리된 Ubuntu 기반 머신에서 실행됩니다.

GitHub 또는 GitLab과 연결될 수 있고, 연결된 레포에서 클론한 뒤 별도 브랜치에서 작업하고 변경 사항을 해당 레포로 푸시합니다.

## 검증

여기서 의심이 하나 생깁니다. _이렇게 맡겨 두면 코드가 엉뚱하게 작성되거나 원하지 않는 결과로 끝나지 않을까?_  
그래서 모델에게는 자신의 작업이 올바른지 검증할 수단이 필요합니다.

에이전트가 코드를 깨뜨리지 않고 계속 작업하게 하려면, **검증 가능해야** 합니다. 프로젝트에 테스트, 린트 같은 검증 수단이 있으면, 에이전트가 실수를 고쳐 가며 올바른 결과를 낼 때까지 스스로 돌 수 있습니다.
그래서 저는 프로젝트 초기에 포맷, 린트, 테스트를 도는 CI부터 붙입니다.
그 이후에는 에이전트가 작업한 모든 결과물이 PR에서 검증을 거치므로, 에이전트가 회귀를 일으키지 않도록 보장할 수 있습니다.

## 처음부터 최적화하지 않습니다

코딩 에이전트를 어떻게 쓰는 게 최적인지, 요즘 워크플로우와 전략 논의가 많습니다.

저는 **처음부터 과도하게 최적화하지 않는 쪽**을 권합니다.

- 린트, 포맷팅, 테스트 등 초기 규칙 설정 없이 시작해도 됩니다
- 에이전트가 반복적으로 잘못 이해한 경우에 규칙을 추가해도 됩니다
- 워크플로우는 실제로 작업하면서 자연스럽게 점진적으로 형성될 것입니다

## 정리

처음에는 낯설고, 과거에 소프트웨어를 만들던 방식과 많이 다르게 느껴질 것입니다. 하지만 지금이 이 도구를 손에 익힐 때입니다.

다음 작업 하나만 Plan 모드로 시작해 보면, 계획을 먼저 세우는 값이 그 자리에서 드러납니다.

## 참고 자료

- [Lee Robinson - Building low-level software with only coding agents](https://leerob.com/pixo)
- [Cursor 공식 문서](https://docs.cursor.com/ko)
